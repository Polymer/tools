{
  "name": "polymer-tools",
  "private": true,
  "description": "Polymer tooling monorepo!",
  "homepage": "https://github.com/Polymer/tools",
  "bugs": "https://github.com/Polymer/tools/issues",
  "license": "BSD-3-Clause",
  "repository": {
    "type": "git",
    "url": "https://github.com/Polymer/tools.git"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --stream",
    "test": "npm run build && npm run test:unit",
    "test:integration": "lerna run test:integration --stream --parallel",
    "test:integration:windows": "lerna run test:integration --stream --parallel --ignore web-component-tester",
    "test:unit": "lerna run test:unit --stream --parallel",
    "test:unit:windows": "lerna run test:unit --stream --parallel --ignore polyserve --ignore web-component-tester"
  },
  "devDependencies": {
    "lerna": "^2.10.1"
  },
  "engines": {
    "node": ">=6"
  }
}
